## 総合設定ファイルとして、/etc/canna2imm32rc にないと起動しません。

# IME.. どれをつかうか。
# ~/.canna2imm32rc の場合は、そのユーザーがどの IME を使うかを指定するが、
# /etc/canna2imm32rc の場合は、~/.canna2imm32rc がない場合にどの IME を
# デフォルトで使うか、を指定する。
# 現状ではIMM32 のみ有効。
IME=IMM32

